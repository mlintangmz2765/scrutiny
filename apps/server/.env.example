# SQLite database. Relative paths resolve from apps/server/prisma/ (the schema
# directory), so this points at <repo-root>/data/scrutiny.db.
DATABASE_URL="file:../../../data/scrutiny.db"

# HTTP port for the API server.
PORT=3001
